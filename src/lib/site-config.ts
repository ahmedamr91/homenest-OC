// Central site configuration values
export const SITE_NAME = "HOMENEST";
export const ADMIN_EMAIL = process.env.ADMIN_EMAIL || "admin@homenest.shop";
// WhatsApp number for the store (international format, digits only).
// Set STORE_WHATSAPP in .env — e.g. 201000000000
export const STORE_WHATSAPP = process.env.STORE_WHATSAPP || "";
